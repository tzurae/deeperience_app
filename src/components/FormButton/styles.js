import { StyleSheet } from 'react-native'

export default (StyleSheet.create({
  signin: {
    marginLeft: 10,
    marginRight: 10,
  },
  button: {
    backgroundColor: '#FF3366',
    borderColor: '#FF3366',
  },
}))
